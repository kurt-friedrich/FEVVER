<%= simple_form_for @comment, url: band_song_comments_path(@band, @song), remote:true do |f| %>
  <%= f.button :submit, 'POST', id: "comment-form-submit" %>
  <%= f.input :body, label: false, placeholder: 'leave a comment' %>
<% end %>
