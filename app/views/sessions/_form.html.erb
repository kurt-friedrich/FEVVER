<%= simple_form_for :session, url: login_create_path do |f| %>
    <%= f.input :email, label: 'email', autofocus: true, required: false %>
    <%= f.input :password, label: 'password', required: false %>
    <%= f.button :submit, "LOG IN" %>
<% end %>
